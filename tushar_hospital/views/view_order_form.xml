<?xml version="1.0" encoding="utf-8"?>
<odoo><data>

    <record id="sale_order_view_inherited" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                    <field name="note"/>
            </xpath>

            <xpath expr="//header" position="inside">
                <button name="action_copy_sale_lines_to_custom"
                        string="Copy Custom Lines"
                        type="object"
                        />
                <button name="action_open_custom_line_wizard"
                        string="View Custom Lines"
                        type="object"
                        />
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Custom Lines">
                    <field name="custom_order_line_ids">
                        <list editable="bottom">
                            <field name="product_id"/>
                            <field name="product_uom_qty"/>
                            <field name="qty_delivered"/>
                            <field name="qty_invoiced"/>
                            <field name="price_unit"/>
                            <field name="price_subtotal"/>
                        </list>
                    </field>
                </page>
            </xpath>

        </field>
    </record>

</data></odoo>

